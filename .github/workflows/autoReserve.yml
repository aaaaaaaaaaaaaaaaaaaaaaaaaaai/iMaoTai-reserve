name: Scheduled Job

on:
  push:
  schedule:
    - cron: '*/6 * * * *'  # 每天午夜执行
env:
  TZ: Asia/Shanghai

jobs:
  scheduled-job:
    runs-on: ubuntu-latest
    steps:
    - name: Run a one-line script
      run: echo Hello, World!
